# The complete RAG service: LightRAG orchestration, local embeddings, cloud LLM.
lightrag-hku==1.4.9
fastembed==0.7.4          # local ONNX embeddings — no model server needed
fastapi==0.118.0
uvicorn[standard]==0.37.0
ollama==0.6.0             # talks to Ollama Cloud for generation only
numpy<3
aiohttp==3.10.11
